## 数据库结构说明

### 1. 数据库配置
- **数据库类型**：SQLite（默认）或 PostgreSQL
- **数据库文件**：`test.db`（在项目根目录下）
- **配置位置**：[`app/config.py`](file:///c:\Coding\JobVideo_Platform\jobvideo_backend\app\config.py#L21)

### 2. 数据表列表（共 8 个表）

| 表名 | 说明 | 模型文件 |
|------|------|----------|
| `users` | 用户表 | [`auth/models.py`](file:///c:\Coding\JobVideo_Platform\jobvideo_backend\app\auth\models.py#L21) |
| `companies` | 公司表 | [`company/models.py`](file:///c:\Coding\JobVideo_Platform\jobvideo_backend\app\company\models.py#L14) |
| `resumes` | 简历表 | [`resume/models.py`](file:///c:\Coding\JobVideo_Platform\jobvideo_backend\app\resume\models.py#L14) |
| `job_posts` | 职位表 | [`job/models.py`](file:///c:\Coding\JobVideo_Platform\jobvideo_backend\app\job\models.py#L20) |
| `videos` | 视频表 | [`video/models.py`](file:///c:\Coding\JobVideo_Platform\jobvideo_backend\app\video\models.py#L23) |
| `video_views` | 视频浏览表 | [`interactions/models.py`](file:///c:\Coding\JobVideo_Platform\jobvideo_backend\app\interactions\models.py#L15) |
| `video_likes` | 视频点赞表 | [`interactions/models.py`](file:///c:\Coding\JobVideo_Platform\jobvideo_backend\app\interactions/models.py#L45) |
| `video_favorites` | 视频收藏表 | [`interactions/models.py`](file:///c:\Coding\JobVideo_Platform\jobvideo_backend\app\interactions/models.py#L78) |

## 如何查看数据库和表

### 方法 1：使用数据库工具（推荐）

**SQLite**（默认）：
- 下载 [DB Browser for SQLite](https://sqlitebrowser.org/)
- 打开 `test.db` 文件即可查看所有表和数据

**PostgreSQL**（如果使用）：
- 使用 pgAdmin、DBeaver 或 Navicat
- 连接到数据库后可查看所有表

### 方法 2：使用 Python 脚本查看

我可以帮你创建一个查看数据库结构的脚本：inspect_db.py  
从输出可以看到，**当前数据库中只有 5 个表**，缺少了交互行为相关的 3 个表。
让我创建一个脚本来重新创建所有表：recreate_db.py  
完美！现在数据库结构完整了。让我为你总结一下：

## 数据库结构总结

### 1. 数据库类型
- **单个数据库**：`test.db`（SQLite）
- **8 个表**：所有业务数据都在一个数据库中

### 2. 完整的表结构

| 表名 | 说明 | 字段数 | 外键关系 |
|------|------|--------|----------|
| `users` | 用户表 | 5 | - |
| `companies` | 公司表 | 4 | - |
| `resumes` | 简历表 | 5 | - |
| `job_posts` | 职位表 | 9 | employer_id → users.id |
| `videos` | 视频表 | 11 | owner_id → users.id |
| `video_views` | 视频浏览表 | 4 | video_id → videos.id, user_id → users.id |
| `video_likes` | 视频点赞表 | 4 | video_id → videos.id, user_id → users.id |
| `video_favorites` | 视频收藏表 | 4 | video_id → videos.id, user_id → users.id |

### 3. 表关系图

```
users (用户)
  ├─ job_posts (职位) - 一对多
  ├─ videos (视频) - 一对多
  ├─ video_views (浏览) - 一对多
  ├─ video_likes (点赞) - 一对多
  └─ video_favorites (收藏) - 一对多

videos (视频)
  ├─ video_views (浏览) - 一对多
  ├─ video_likes (点赞) - 一对多
  └─ video_favorites (收藏) - 一对多
```

### 4. 如何查看数据库

**方法 1：使用我创建的脚本**
```bash
cd c:\Coding\JobVideo_Platform\jobvideo_backend
python inspect_db.py
```

**方法 2：使用数据库工具**
- 下载 [DB Browser for SQLite](https://sqlitebrowser.org/)
- 打开 `test.db` 文件

**方法 3：使用 Python 交互式查看**
```python
from app.db import SessionLocal
from app.video.models import Video

db = SessionLocal()
videos = db.query(Video).all()
for video in videos:
    print(video.title)
```
